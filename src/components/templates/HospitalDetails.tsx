import React from "react";
import Navbar from "../layouts/Navbar";

type Props = {};

const HospitalDetails = (props: Props) => {
  return (
    <div>
      <h2 className="font-bold">تخصیص تخت</h2>
      <Navbar />
    </div>
  );
};

export default HospitalDetails;
